---
title: "Biondicadores convencionales"
subtitle: "*E. coli* y enterococos intestinales (EEII)"
author:
  - name: Vera Gómez-Limón Gallardo
    orcid: 0009-0005-4889-3412
---

```{r}
#| label: tidy
#| message: false

library(tidyverse)

# Lee la hoja de cálculo y elimina celdas sin información
datos <- readxl::read_xlsx(
  "data/Muestreo Confital 2025_ Datos fecales Hidrotecnia Sanidad ULPGC.xlsx",
  col_names = c("Fecha", "T", "Marea", "Hora",
                "E.coli.Hidrotecnia", "E.coli.Nayade",
                "EEII.Hidrotecnia", "EEII.Nayade",
                "skip", "skip", "Muestra.ULPGC"),
  col_types = c("guess", "guess", "text", "guess",
                "guess", "guess",
                "guess", "guess",
                "skip", "skip", "text"),
  range = "A3:K23")

# Fusiona las columnas de fecha y hora en un único instante de tiempo
datos <- datos |> mutate(
  Fecha = if_else(
    is.na(Hora), Fecha, Fecha + hour(Hora)*60*60 + minute(Hora)*60
  ),
  .keep = "unused"
)

# Aproxima los valores "<10" a 10.
datos <- datos |> mutate(
  E.coli.Hidrotecnia = parse_number(E.coli.Hidrotecnia)
)

```

```{r}
#| label: transform

# Junta las columnas con valores UFC en una única variable.
datos_valor <- datos |> pivot_longer(
  starts_with("E"), names_to = c("Parametro"), values_to = "Valor"
)

# Especifica las categorías (Indicador y laboratorio) para cada valor UFC.
datos_valor <- datos_valor |> separate_wider_regex(
  Parametro,
  c(Indicador = "EEII|E\\.coli", "\\.", Sujeto = "Hidrotecnia|Nayade")
)

# Elimina valores perdidos
datos_valor <- datos_valor |> filter(!is.na(Valor))

# Ensancha a una columna por indicador
datos_indicador <- datos_valor |> pivot_wider(
  names_from = Indicador,
  values_from = Valor
)

# Ensancha los valores en valores de sujeto
datos_par <- datos_valor |> pivot_wider(
  names_from = Sujeto,
  values_from = Valor
)

# Elimina los valores desemparejados
datos_par <- datos_par |> filter(!is.na(Hidrotecnia), !is.na(Nayade))

# Transforma a un valor por fila
datos_par_valor <- datos_par |> pivot_longer(
  c("Hidrotecnia", "Nayade"), names_to = "Sujeto", values_to = "Valor"
)

# Calcula la diferencia entre sujetos
datos_diferencia <- datos |> mutate(
  Diferencia.EEII = EEII.Nayade - EEII.Hidrotecnia,
  Diferencia.E.coli = E.coli.Nayade - E.coli.Nayade
)

# Filtra valores perdidos
datos_diferencia <- datos_diferencia |> filter(!is.na(Diferencia.EEII))

# ---- Límites de calidad de agua insuficiente ----

# Crea una tibble con los indicadores y su valor límite de insuficiencia.
limites <- tribble(
  ~Indicador, ~Limite,        ~Valor,
  "E.coli",   "Insuficiente", 500,
  "EEII",     "Insuficiente", 185,
  "E.coli",   "Excelente",    250,
  "EEII",     "Excelente",    100
)
```

```{r}
#| label: test

library(infer)

set.seed(81)

# ---- t tests ----

# Calcula el estadístico t para los datos reales
t_real_eeii_sujeto <- datos_diferencia |> 
  specify(response = Diferencia.EEII) |> 
  calculate(stat = "mean")

# Calcula 2000 estadísticos t para datos simulados bajo hipótesis nula
ts_hipoteticas_eeii_sujeto <- datos_diferencia |> 
  specify(response = Diferencia.EEII) |> 
  hypothesize(null = "paired independence") |> 
  generate(reps = 2000, type = "permute") |> 
  calculate(stat = "mean")

# Hay diferencias significativas de enterococos según el sujeto de muestreo?
ttest_eeii_sujeto <- t.test(
  datos_par[datos_par$Indicador == "EEII",]$Nayade,
  datos_par[datos_par$Indicador == "EEII",]$Hidrotecnia,
  paired = TRUE
)

# Hay diferencias significativas de E.coli según el sujeto de muestreo?
ttest_ecoli_sujeto <- t.test(
  datos_par[datos_par$Indicador == "E.coli",]$Nayade,
  datos_par[datos_par$Indicador == "E.coli",]$Hidrotecnia,
  paired = TRUE
)

# ---- ANOVA ----

# Calcula el estadístico F para los datos reales
f_real <- datos_indicador |>
  specify(response = EEII, explanatory = Marea) |> 
  calculate(stat = "F")

# Calcula 2000 estadísticos F para datos simulados bajo hipótesis nula
fs_hipoteticas <- datos_indicador |> 
  specify(response = EEII, explanatory = Marea) |> 
  hypothesise(null = "independence") |> 
  generate(reps = 1000, type = "permute") |> 
  calculate(stat = "F")

# Hay diferencias significativas de EEII según la fase de la marea?
aov_eeii_marea <- aov(
  Valor ~ Marea,
  data = datos_valor |> filter(Indicador == "EEII")
)

# Hay diferencias significativas de E.coli según la fase de la marea?
aov_ecoli_marea <- aov(
  Valor ~ Marea,
  data = datos_valor |> filter(Indicador == "E.coli")
)
```

```{r}
#| label: visualize

# Semilla para reproducibilidad de los jitter
set.seed(26)

# Vista general verano 2025
fig_vista_general <- datos_par_valor |> 
  ggplot(aes(x = Fecha, y = Valor, fill = Sujeto)) +
  geom_area(
    color = "black", position = position_dodge(width = 0), alpha = .6
  ) +
  geom_point(aes(shape = Sujeto)) +
  geom_hline(
    data = limites,
    mapping = aes(yintercept = Valor, color = fct_inorder(Limite))
  ) +
  facet_wrap(~Indicador, nrow = 2, scale = "free_y") +
  scale_x_datetime(date_labels = "%y-%m-%d") +
  scale_fill_manual(values = c("#2A788E", "#7AD151")) +
  labs(
    title = "Línea temporal de los indicadores fecales",
    subtitle = "Mayor detección de EEII por Náyade",
    color = "Límite",
    x = "",
    y = "UFC/100 ml"
  )

# Indicadores fecales según el sujeto
fig_sujeto <- datos_par_valor |>
  ggplot(aes(x = Sujeto, y = Valor)) +
  geom_boxplot(aes(fill = Sujeto), show.legend = F, outliers = F) +
  geom_jitter(alpha = .7, width = .3) +
  facet_wrap(~Indicador, scale = "free_y") +
  scale_fill_manual(values = c("#2A788E", "#7AD151")) +
  labs(
    title = "Indicadores fecales según el sujeto muestreador",
    subtitle = "Mayor variabilidad en las muestras de Náyade",
    y = "UFC/100 ml"
  )

# Indicadores fecales según la marea
fig_mareas <- datos_valor |> 
  ggplot(aes(x = Marea, y = Valor)) +
  geom_boxplot(aes(fill = Marea), show.legend = F, outliers = F) +
  geom_jitter(alpha = .7, width = .3, size = 1) +
  facet_wrap(~Indicador, scale = "free_y") +
  scale_fill_manual(values = c("#DEEBF7", "#9ECAE1", "#4292C6", "#9ECAE1")) +
  labs(
    title = "Indicadores fecales según la fase de la marea",
    y = "UFC/100 ml"
  )

# Estadístico t para la variación de EEII según el sujeto
fig_t_eeii_sujeto <- visualize(ts_hipoteticas_eeii_sujeto) +
  shade_p_value(obs_stat = t_real_eeii_sujeto, direction = "both") +
  annotate(
    "text",
    x = 125, y = 150,
    label = paste(
      "p.value\n",
      get_p_value(
        ts_hipoteticas_eeii_sujeto,
        t_real_eeii_sujeto,
        direction = "both"
      )
    )
    ) +
  labs(
    title = "T de Student emparejada. Diferencia de EEII según sujeto",
    subtitle = "Diferencias probablemente debidas al azar (> 0,05)",
    x = "estadístico t",
    y = "simulaciones"
  )

# ANOVA para la variación de EEII con la marea
fig_f_eeii_marea <- visualise(fs_hipoteticas) +
  shade_p_value(obs_stat = f_real, direction = "greater") +
  annotate(
    "text",
    x = 6, y = 200,
    label = paste(
      "p.value\n",
      get_p_value(fs_hipoteticas, f_real, direction = "greater")
      )
    ) +
  labs(
    title = "ANOVA para la variabilidad de EEII con la marea",
    subtitle = "Sin diferencias significativas entre grupos (> 0,05)",
    x = "estadístico F",
    y = "simulaciones"
  )
```

## Vista general

Los episodios periódicos de contaminación fecal en la playa de El Confital se monitorearon durante el verano 2025. La bacteria *E.coli* se detectó en todas las muestras dentro de los valores de calidad excelente del agua establecidos por la normativa[@boe2007].

El Ministerio de Sanidad (Náyade) y AT Hidrotecnia SL (Hidrotecnia) son los dos sujetos responsables de monitorear los indicadores de contaminación fecal. Atendiendo a la distribución de resultados obtenidos (@fig-general), observamos una mayor dispersión según Náyade que respecto de Hidrotecnia. Además, únicamente los valores de Náyade superaron en 2 ocasiones el umbral de calidad de agua insuficiente para EEII.

La problemática reside en los repuntes de EEII, siendo los meses de julio y septiembre 2025 donde se dieron los episodios de calidad insuficiente del agua.

```{r}
#| label: fig-general
fig_vista_general
```

## Sujeto muestreador

```{r}
#| label: fig-sujeto
fig_sujeto
```

Para estimar estadísticamente si existen diferencias significativas entre los resultados obtenidos por ambos sujetos, utilizamos la prueba de la *t* de *Student* para los valores emparejados por la misma muestra de agua. En la @fig-t-eeii-sujeto se observa como la diferencia de EEII entre Náyade e Hidrotecnia no es significativa con los datos disponibles. **Probablemente la mayor detección de Náyade se deba al azar** (*p* valor > 0,05).

```{r}
#| label: fig-t-eeii-sujeto
fig_t_eeii_sujeto
```

## Ciclo de mareas

```{r}
#| label: fig-mareas
fig_mareas
```

Las muestras fueron tomadas durante diferentes fases del ciclo de mareas como un posible factor de control. Aplicando el análisis de la varianza (ANOVA), estudiamos la variabilidad de EEII con el estado de la marea. En @fig-f-eeii-marea se demuestra que el muestreo **no detectó diferencias significativas** en la concentración de EEII según el estado de la marea.

```{r}
#| label: fig-f-eeii-marea
fig_f_eeii_marea
```

